@model IEnumerable<Domain.CompanyWorkerPosition>

@{
    ViewData["Title"] = "Index";
}

<h2>@Resources.Common.Index</h2>

<p>
    <a asp-action="Create">@Resources.Common.CreateNew</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.PositionName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.Raw(item.PositionName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CompanyWorkerPositionId">@Resources.Common.Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CompanyWorkerPositionId">@Resources.Common.Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CompanyWorkerPositionId">@Resources.Common.Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
