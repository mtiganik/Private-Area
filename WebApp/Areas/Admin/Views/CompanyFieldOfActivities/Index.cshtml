@model IEnumerable<Domain.CompanyFieldOfActivity>

@{
    ViewData["Title"] = "Index";
}

<h2>@Resources.Common.Index</h2>

<p>
    <a asp-action="Create">@Resources.Common.CreateNew</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ActivityName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.Raw(item.ActivityName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CompanyFieldOfActivityId">@Resources.Common.Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CompanyFieldOfActivityId">@Resources.Common.Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CompanyFieldOfActivityId">@Resources.Common.Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
