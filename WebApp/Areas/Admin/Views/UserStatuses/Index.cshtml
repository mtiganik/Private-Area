@model IEnumerable<Domain.UserStatus>

@{
    ViewData["Title"] = "Index";
}

<h2>@Resources.Common.Index</h2>

<p>
    <a asp-action="Create">@Resources.Common.CreateNew</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.UserStatusName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.Raw(item.UserStatusName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserStatusId">@Resources.Common.Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserStatusId">@Resources.Common.Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserStatusId">@Resources.Common.Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
