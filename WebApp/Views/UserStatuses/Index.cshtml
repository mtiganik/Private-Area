@model IEnumerable<Domain.UserStatus>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.UserStatusName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UserStatusNameEst)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserStatusName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserStatusNameEst)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserStatusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserStatusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserStatusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
