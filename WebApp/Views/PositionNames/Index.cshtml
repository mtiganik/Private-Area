@model IEnumerable<Domain.PositionName>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.PositionNameEng)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PositionNameEst)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PositionNameEng)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PositionNameEst)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PositionNameId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PositionNameId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PositionNameId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
