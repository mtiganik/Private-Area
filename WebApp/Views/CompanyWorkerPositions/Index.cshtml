@model IEnumerable<Domain.CompanyWorkerPosition>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.PositionName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PositionNameEst)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PositionName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PositionNameEst)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CompanyWorkerPositionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CompanyWorkerPositionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CompanyWorkerPositionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
