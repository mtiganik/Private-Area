@model IEnumerable<Domain.ContactType>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ContactTypeName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ContactTypeNameEst)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ContactTypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactTypeNameEst)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContactTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ContactTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ContactTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
